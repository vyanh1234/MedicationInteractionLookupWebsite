const $=s=>document.querySelector(s), app=$('#app'), token=()=>localStorage.token, authDialog=$('#authDialog');
let me=JSON.parse(localStorage.getItem('user')||'null'), cache={};
async function api(path,opt={}){const headers={'Content-Type':'application/json',...(token()?{Authorization:'Bearer '+token()}:{})};const r=await fetch('/api'+path,{...opt,headers:{...headers,...opt.headers}});const data=await r.json().catch(()=>({}));if(!r.ok)throw Error(data.message||'Không thể kết nối máy chủ');return data}
function toast(msg){const x=$('#toast');x.textContent=msg;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),2500)}
function updateNav(){document.body.classList.toggle('logged',!!me);document.querySelectorAll('.private').forEach(x=>x.style.display=me?.role==='BENH_NHAN'?'':'none');document.querySelectorAll('.staff').forEach(x=>x.style.display=me&&me.role!=='BENH_NHAN'?'':'none');$('#account').innerHTML=me?`<div class="avatar">${me.fullName?.[0]||'U'}</div><span>${me.fullName}</span><button class="ghost" onclick="logout()">Thoát</button>`:`<button class="ghost" onclick="showLogin()">Đăng nhập</button><button onclick="showRegister()">Đăng ký</button>`}
function showLogin(){authDialog.showModal();$('#authContent').innerHTML=`<h2>Chào mừng trở lại</h2><p class="muted">Đăng nhập để nhận cảnh báo theo hồ sơ sức khỏe.</p><form onsubmit="login(event)"><div class="field"><label>Tên đăng nhập</label><input name="username" required value="patient"></div><div class="field"><label>Mật khẩu</label><input name="password" type="password" required value="123456"></div><div class="demo">Demo: patient / doctor / admin — mật khẩu <b>123456</b></div><button style="width:100%">Đăng nhập</button></form><div id="googleArea"><div class="divider">hoặc</div><button class="google-btn" onclick="googleStart()">G&nbsp; Đăng nhập bằng Google</button></div><p>Chưa có tài khoản? <a href="#" onclick="showRegister()">Đăng ký</a></p>`;configureGoogleButton()}
function showRegister(){authDialog.showModal();$('#authContent').innerHTML=`<h2>Tạo tài khoản</h2><form onsubmit="register(event)"><div class="field"><label>Họ và tên</label><input name="fullName" required></div><div class="field"><label>Email hoặc tên đăng nhập</label><input name="username" minlength="4" required></div><div class="field"><label>Mật khẩu</label><input name="password" type="password" minlength="6" required></div><button style="width:100%">Đăng ký</button></form><div id="googleArea"><div class="divider">hoặc</div><button class="google-btn" onclick="googleStart()">G&nbsp; Tiếp tục với Google</button></div>`;configureGoogleButton()}
async function login(e){e.preventDefault();try{const body=Object.fromEntries(new FormData(e.target));const x=await api('/auth/login',{method:'POST',body:JSON.stringify(body)});localStorage.token=x.token;localStorage.user=JSON.stringify(x.user);me=x.user;authDialog.close();updateNav();toast('Đăng nhập thành công');location.hash=me.role==='BENH_NHAN'?'lookup':'admin'}catch(e){toast(e.message)}}
async function register(e){e.preventDefault();try{await api('/auth/register',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});toast('Đăng ký thành công');showLogin()}catch(e){toast(e.message)}}
async function configureGoogleButton(){try{const c=await api('/auth/google',{method:'POST'});cache.google=c;const area=$('#googleArea');if(area&&!c.enabled)area.innerHTML='<p class="muted" style="text-align:center">Chức năng đăng nhập bằng Google hiện chưa được kích hoạt.</p>'}catch{const area=$('#googleArea');if(area)area.style.display='none'}}
async function googleStart(){try{const c=cache.google||await api('/auth/google',{method:'POST'});if(!c.enabled){toast('Chức năng đăng nhập bằng Google hiện chưa được kích hoạt.');return}if(!window.google){await new Promise((ok,no)=>{const s=document.createElement('script');s.src='https://accounts.google.com/gsi/client';s.onload=ok;s.onerror=no;document.head.appendChild(s)})}google.accounts.id.initialize({client_id:c.clientId,callback:handleGoogleCredential});google.accounts.id.prompt()}catch(e){toast('Không thể kết nối dịch vụ đăng nhập Google lúc này.')}}
async function handleGoogleCredential(x){try{const r=await api('/auth/google/callback',{method:'POST',body:JSON.stringify({credential:x.credential})});localStorage.token=r.token;localStorage.user=JSON.stringify(r.user);me=r.user;authDialog.close();updateNav();toast('Đăng nhập Google thành công');location.hash='lookup'}catch(e){toast(e.message)}}
async function logout(){try{await api('/auth/logout',{method:'POST'})}catch{}localStorage.clear();me=null;updateNav();location.hash='home'}
function home(){app.innerHTML=`<section class="hero"><div><div class="eyebrow">Sức khỏe chủ động mỗi ngày</div><h1>Kiểm tra thuốc.<br><span style="color:var(--green)">An tâm hơn</span> khi sử dụng.</h1><p>Tra cứu cảnh báo thuốc dựa trên bệnh nền, dị ứng và các thuốc bạn đang dùng — nhanh chóng, rõ ràng và cá nhân hóa.</p><button onclick="location.hash='lookup'">Tra cứu ngay →</button></div><div class="hero-art"><div class="pill"></div><div class="float-card warn">⚠️ <b>Cảnh báo tương tác</b><br><small>Phát hiện và giải thích rõ ràng</small></div><div class="float-card ok">✓ Dữ liệu đã được duyệt</div></div></section><section class="section"><div class="eyebrow">MedCheck hỗ trợ bạn</div><h2>Ba lớp kiểm tra trong một lần tra cứu</h2><div class="features"><div class="card"><div class="icon">♙</div><h3>Bệnh nền</h3><p>Đối chiếu hoạt chất với bệnh nền trong hồ sơ của bạn.</p></div><div class="card"><div class="icon">⚕</div><h3>Dị ứng</h3><p>Nhận cảnh báo khi thuốc chứa hoạt chất có nguy cơ dị ứng.</p></div><div class="card"><div class="icon">⇄</div><h3>Tương tác thuốc</h3><p>Kiểm tra thuốc mới với danh sách thuốc đang sử dụng.</p></div></div></section>`}
let suggestTimer;
async function lookup(){app.innerHTML=`<div class="page-head"><h1>Tra cứu thuốc an toàn</h1><p class="muted">Nhập từ 2 ký tự để gợi ý theo tên thuốc, tên khác hoặc hoạt chất.</p></div><section class="section"><div class="search-wrap"><div class="search-box"><input id="q" autocomplete="off" placeholder="Tìm thuốc..." oninput="onDrugInput(this.value)" onkeydown="if(event.key==='Escape')hideSuggestions()"><button onclick="loadDrugs($('#q').value)">Tìm kiếm</button></div><div id="suggestions" class="suggestions" hidden></div></div><div id="drugs" class="drug-list"><div class="empty">Đang tải…</div></div><div id="result"></div></section>`;loadDrugs('')}
function onDrugInput(value){clearTimeout(suggestTimer);if(value.trim().length<2){hideSuggestions();return}const box=$('#suggestions');box.hidden=false;box.innerHTML='<div class="suggestion muted">Đang tìm thuốc…</div>';suggestTimer=setTimeout(()=>loadSuggestions(value),300)}
async function loadSuggestions(value){try{const r=await api('/drugs/autocomplete?keyword='+encodeURIComponent(value));const box=$('#suggestions');if(!box)return;box.innerHTML=r.items.length?r.items.map(x=>`<div class="suggestion" data-id="${x.id}"><b>${x.tenThuoc}</b>${x.canKeDon?'<span class="tag" style="margin-left:8px">Kê đơn</span>':''}<small>Hoạt chất: ${x.hoatChat||'Đang cập nhật'} · ${x.dangBaoChe||'Chưa rõ dạng'} · ${x.duongDung||'Chưa rõ đường dùng'}</small></div>`).join(''):'<div class="suggestion muted">Không tìm thấy thuốc phù hợp</div>';box.querySelectorAll('[data-id]').forEach((el,i)=>el.onclick=()=>selectSuggestion(r.items[i]));box.hidden=false}catch(e){hideSuggestions()}}
function hideSuggestions(){const x=$('#suggestions');if(x)x.hidden=true}
function selectSuggestion(x){$('#q').value=x.tenThuoc;hideSuggestions();checkDrug(x.id)}
async function loadDrugs(q){try{const xs=await api('/drugs?keyword='+encodeURIComponent(q));cache.drugs=xs;$('#drugs').innerHTML=xs.length?xs.map(x=>`<article class="card drug" onclick="checkDrug(${x.id})"><span class="tag">${x.dosageForm||'Thuốc'}</span><h3>${x.name}</h3><div class="muted">${x.ingredients||''} · ${x.strength||''}</div><p>${x.description||''}</p><b style="color:var(--green)">Kiểm tra an toàn →</b></article>`).join(''):`<div class="empty">Không tìm thấy thuốc phù hợp.</div>`}catch(e){toast(e.message)}}
async function checkDrug(id){if(!me){showLogin();return}if(me.role!=='BENH_NHAN'){toast('Hãy đăng nhập bằng tài khoản bệnh nhân để tra cứu cá nhân hóa');return}try{const x=await api('/lookup/drug',{method:'POST',body:JSON.stringify({drugId:id})});cache.lookup=x;$('#drugs').style.display='none';$('#result').innerHTML=`<button class="ghost" onclick="$('#drugs').style.display='grid';$('#result').innerHTML=''">← Tra cứu thuốc khác</button><article class="card" style="margin-top:16px"><div class="result-head"><div><div class="eyebrow">Kết quả tra cứu</div><h2>${x.drug.name}</h2><p class="muted">Hoạt chất: ${x.ingredients.map(i=>i.name+' '+(i.strength||'')).join(', ')}</p></div><span class="severity ${x.highestSeverity}">${labelSeverity(x.highestSeverity)}</span></div>${x.warnings.length?x.warnings.map((w,i)=>`<div class="card warning ${w.severity}"><h3>${w.type}: ${w.subject}<span class="severity ${w.severity}">${labelSeverity(w.severity)}</span></h3><p>${w.content}</p><b>Khuyến nghị</b><p>${w.recommendation}</p>${w.source?`<small class="muted">Nguồn: ${w.source}</small>`:''}<div><button class="ghost" style="margin-top:10px" onclick="requestAi(${i})">✦ Yêu cầu AI giải thích dễ hiểu</button></div></div>`).join(''):`<div class="card" style="background:var(--mint)"><h3>✓ Chưa phát hiện cảnh báo trong dữ liệu hiện có</h3><p>Điều này không khẳng định thuốc hoàn toàn an toàn. Hãy hỏi nhân viên y tế khi cần.</p></div>`}<div class="disclaimer">ⓘ ${x.disclaimer}</div></article>`;$('#result').scrollIntoView({behavior:'smooth'})}catch(e){toast(e.message)}}
async function requestAi(index){const w=cache.lookup.warnings[index],source=({'Bệnh nền':'QuyTacCanhBao','Dị ứng':'CanhBaoDiUng_HoatChat','Tương tác':'TuongTacHoatChat'})[w.type];try{const r=await api('/ai/generate',{method:'POST',body:JSON.stringify({question:'Giải thích cảnh báo này dễ hiểu',input:w.content,source})});toast(r.message)}catch(e){toast(e.message)}}
function labelSeverity(s){return({Cao:'Nguy cơ cao',TrungBinh:'Cần thận trọng',Thap:'Nguy cơ thấp',ChuaDuDuLieu:'Chưa đủ dữ liệu'})[s]||s}
async function profile(){if(!guardPatient())return;app.innerHTML=`<div class="page-head"><h1>Hồ sơ sức khỏe</h1><p class="muted">Thông tin này giúp kết quả tra cứu phù hợp hơn với bạn.</p></div><section class="section"><div class="tabs"><button class="active" onclick="profileTab('basic',this)">Thông tin cá nhân</button><button onclick="profileTab('diseases',this)">Bệnh nền</button><button onclick="profileTab('allergies',this)">Dị ứng</button><button onclick="profileTab('drugs',this)">Thuốc đang dùng</button></div><div id="profileBody"></div></section>`;profileTab('basic',$('.tabs button'))}
function guardPatient(){if(!me){showLogin();return false}if(me.role!=='BENH_NHAN'){location.hash='admin';return false}return true}
async function profileTab(tab,btn){document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));btn?.classList.add('active');const body=$('#profileBody');body.innerHTML='<div class="empty">Đang tải…</div>';try{if(tab==='basic'){const p=await api('/patient/profile');body.innerHTML=`<form class="card" onsubmit="saveProfile(event)"><div class="row"><div class="field"><label>Họ tên</label><input name="fullName" value="${p.HoTen||''}"></div><div class="field"><label>Ngày sinh</label><input type="date" name="birthDate" value="${p.NgaySinh||''}"></div><div class="field"><label>Giới tính</label><select name="gender"><option>${p.GioiTinh||'Nam'}</option><option>Nam</option><option>Nữ</option><option>Khác</option></select></div><div class="field"><label>Nhóm máu</label><input name="bloodType" value="${p.NhomMau||''}"></div><div class="field"><label>Chiều cao (cm)</label><input name="height" type="number" step=".1" value="${p.ChieuCao||''}"></div><div class="field"><label>Cân nặng (kg)</label><input name="weight" type="number" step=".1" value="${p.CanNang||''}"></div></div><div class="field"><label>Địa chỉ</label><input name="address" value="${p.DiaChi||''}"></div><div class="field"><label>Ghi chú sức khỏe</label><textarea name="note">${p.GhiChu||''}</textarea></div><button>Lưu thay đổi</button></form>`}else await renderHealthList(tab)}catch(e){toast(e.message)}}
async function saveProfile(e){e.preventDefault();await api('/patient/profile',{method:'PUT',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});toast('Đã lưu hồ sơ')}
async function renderHealthList(tab){const cfg={diseases:['/patient/background-diseases','/diseases','Chọn bệnh nền'],allergies:['/patient/allergies','/allergies','Chọn dị ứng'],drugs:['/patient/current-drugs','/drugs','Chọn thuốc']}[tab], [mine,all]=await Promise.all([api(cfg[0]),api(cfg[1])]);$('#profileBody').innerHTML=`<div class="grid"><div class="card"><h2>Đã khai báo</h2>${mine.length?mine.map(x=>`<div class="list-row"><div class="grow"><b>${x.name}</b><div class="muted">${x.note||x.severity||x.dose||''}</div></div><button class="danger" onclick="removeHealth('${tab}',${x.id})">Xóa</button></div>`).join(''):'<div class="empty">Chưa có thông tin</div>'}</div><form class="card" onsubmit="addHealth(event,'${tab}')"><h2>Thêm mới</h2><div class="field"><label>${cfg[2]}</label><select name="${tab==='drugs'?'drugId':'id'}">${all.map(x=>`<option value="${x.id}">${x.name}</option>`).join('')}</select></div>${tab==='drugs'?'<div class="field"><label>Liều dùng</label><input name="dose" placeholder="Ví dụ: 1 viên"></div><div class="field"><label>Tần suất</label><input name="frequency" placeholder="Ví dụ: Sau ăn sáng"></div>':'<div class="field"><label>Ghi chú / mức độ</label><input name="note"></div>'}<button>Thêm vào hồ sơ</button></form></div>`}
async function addHealth(e,tab){e.preventDefault();const paths={diseases:'background-diseases',allergies:'allergies',drugs:'current-drugs'};await api('/patient/'+paths[tab],{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});toast('Đã thêm vào hồ sơ');profileTab(tab,document.querySelectorAll('.tabs button')[{diseases:1,allergies:2,drugs:3}[tab]])}
async function removeHealth(tab,id){const paths={diseases:'background-diseases',allergies:'allergies',drugs:'current-drugs'};await api('/patient/'+paths[tab]+'/'+id,{method:'DELETE'});toast('Đã xóa');profileTab(tab,document.querySelectorAll('.tabs button')[{diseases:1,allergies:2,drugs:3}[tab]])}
async function history(){if(!guardPatient())return;app.innerHTML=`<div class="page-head"><h1>Lịch sử tra cứu</h1></div><section class="section"><div id="history" class="card"><div class="empty">Đang tải…</div></div></section>`;try{const xs=await api('/lookup/history');$('#history').innerHTML=xs.length?xs.map(x=>`<div class="list-row"><div class="icon">⌕</div><div class="grow"><b>${x.drug}</b><div class="muted">${new Date(x.time).toLocaleString('vi-VN')} · ${x.warningCount} cảnh báo</div></div><span class="severity ${x.severity}">${labelSeverity(x.severity)}</span></div>`).join(''):'<div class="empty">Bạn chưa có lần tra cứu nào.</div>'}catch(e){toast(e.message)}}
async function notifications(){if(!guardPatient())return;app.innerHTML=`<div class="page-head"><h1>Thông báo</h1></div><section class="section"><div id="notices" class="card"><div class="empty">Đang tải…</div></div></section>`;try{const xs=await api('/ai/approved');$('#notices').innerHTML=xs.length?xs.map(x=>`<div class="list-row"><div class="icon">✓</div><div class="grow"><b>Giải thích chuyên môn đã được duyệt</b><p>${x.content}</p><small class="muted">Nguồn: ${x.dataSource}</small></div></div>`).join(''):'<div class="empty">Bạn chưa có thông báo mới.</div>'}catch(e){toast(e.message)}}
async function admin(){if(!me){showLogin();return}if(me.role==='BENH_NHAN'){location.hash='lookup';return}if(me.role==='ADMIN')return adminDashboard();professionalDashboard()}
async function professionalDashboard(){app.innerHTML=`<div class="page-head"><h1>Trung tâm chuyên môn</h1><p class="muted">Quản lý quy tắc và duyệt nội dung AI.</p></div><section class="section"><h2>Nội dung AI chờ duyệt</h2><div id="aiReview" class="card"><div class="empty">Đang tải…</div></div><h2 style="margin-top:35px">Quy tắc cảnh báo</h2><div id="rules" class="card"><div class="empty">Đang tải…</div></div></section>`;try{const [r,a]=await Promise.all([api('/warning-rules'),api('/ai/suggestions/pending')]);renderAiReview(a);$('#rules').innerHTML=`<table><thead><tr><th>Hoạt chất</th><th>Bệnh nền</th><th>Mức độ</th><th>Trạng thái</th></tr></thead><tbody>${r.map(x=>`<tr><td>${x.ingredient}</td><td>${x.disease}</td><td><span class="severity ${x.severity}">${labelSeverity(x.severity)}</span></td><td>${x.status}</td></tr>`).join('')}</tbody></table>`}catch(e){toast(e.message)}}
async function adminDashboard(){app.innerHTML=`<div class="page-head"><h1>Dashboard quản trị</h1><p class="muted">Tài khoản, phân quyền, nhật ký và thống kê hệ thống.</p></div><section class="section"><div id="stats" class="stats"></div><div class="grid" style="margin-top:24px"><div class="card"><h2>Thuốc bị cảnh báo nhiều</h2><div id="topDrugs"></div></div><div class="card"><h2>Cảnh báo theo mức độ</h2><div id="warningChart"></div></div></div><h2 style="margin-top:35px">Quản lý tài khoản</h2><div id="users" class="card"></div></section>`;try{const [s,u]=await Promise.all([api('/admin/dashboard/summary'),api('/admin/users')]);$('#stats').innerHTML=[['Tổng tài khoản',s.tongTaiKhoan],['Bệnh nhân',s.tongBenhNhan],['Lượt tra cứu',s.tongLuotTraCuu]].map(x=>`<div class="card stat"><div class="muted">${x[0]}</div><b>${x[1]}</b></div>`).join('');$('#topDrugs').innerHTML=s.thuocBiCanhBaoNhieuNhat.length?s.thuocBiCanhBaoNhieuNhat.map(x=>`<div class="list-row"><span class="grow">${x.tenThuoc}</span><b>${x.soLan}</b></div>`).join(''):'<div class="empty">Chưa có dữ liệu</div>';const max=Math.max(1,...s.canhBaoTheoMucDo.map(x=>x.soLuong));$('#warningChart').innerHTML=s.canhBaoTheoMucDo.map(x=>`<div style="margin:12px 0"><div>${labelSeverity(x.mucDoCanhBao)} — ${x.soLuong}</div><div style="height:10px;background:var(--line);border-radius:9px"><div style="height:100%;width:${x.soLuong/max*100}%;background:var(--green);border-radius:9px"></div></div></div>`).join('')||'<div class="empty">Chưa có dữ liệu</div>';renderUsers(u)}catch(e){toast(e.message)}}
function renderUsers(u){$('#users').innerHTML=`<table><thead><tr><th>Tài khoản</th><th>Vai trò</th><th>Trạng thái</th><th>Thao tác</th></tr></thead><tbody>${u.map(x=>`<tr><td><b>${x.username}</b><br><small>${x.fullName||''}</small></td><td><select onchange="changeRole(${x.id},this.value)">${['BENH_NHAN','DUOC_SI','BAC_SI','ADMIN'].map(r=>`<option ${r===x.role?'selected':''}>${r}</option>`).join('')}</select></td><td>${x.active?'Hoạt động':'Đã khóa'}</td><td><button class="${x.active?'danger':'ghost'}" onclick="toggleUser(${x.id},${x.active})">${x.active?'Khóa':'Mở khóa'}</button></td></tr>`).join('')}</tbody></table>`}
async function changeRole(id,role){try{const r=await api(`/admin/users/${id}/role`,{method:'PUT',body:JSON.stringify({role})});toast(r.message)}catch(e){toast(e.message);adminDashboard()}}
async function toggleUser(id,active){try{const r=await api(`/admin/users/${id}/${active?'lock':'unlock'}`,{method:'PUT'});toast(r.message);adminDashboard()}catch(e){toast(e.message)}}
function renderAiReview(items){$('#aiReview').innerHTML=items.length?items.map(x=>`<div class="card ai-review" style="margin-bottom:12px"><span class="tag">${x.dataSource}</span><p><b>Nội dung gốc:</b> ${x.originalContent}</p><p><b>AI diễn giải:</b> ${x.aiContent}</p><div class="ai-actions"><button onclick="decideAi(${x.id},'approve')">Duyệt</button><button class="danger" onclick="decideAi(${x.id},'reject')">Từ chối</button></div></div>`).join(''):'<div class="empty">Không có nội dung AI chờ duyệt.</div>'}
async function decideAi(id,action){try{const r=await api('/ai/'+action,{method:'POST',body:JSON.stringify({id})});toast(r.message);admin()}catch(e){toast(e.message)}}
function route(){document.querySelectorAll('nav a').forEach(x=>x.classList.toggle('active',x.hash===location.hash));const p=(location.hash||'#home').slice(1);({home,lookup,profile,history,notifications,admin}[p]||home)();scrollTo(0,0)}
window.addEventListener('hashchange',route);updateNav();route();
